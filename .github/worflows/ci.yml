name: Submit to CodeSpell
on:
  push:
    branches:
      - main

jobs:
  submit:
    runs-on: ubuntu-latest
    steps:
      - name: Check out Repository
        uses: actions/checkout@v4

      - name: Submit to CodeSpell
        uses: code-spell/codespell-submission-action@v1
        with:
          content-id: ${{ secrets.BERNARDO_LIMA_ID }}
          language: PYTHON
          username: ${{ secrets.BERNARDO_LIMA }}
          password: ${{ secrets.BERNARDO_LIMA_PASSWORD }}
